{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.m4trust.internal/ai/common/download-reference/1.0.0",
  "title": "M4Trust Short-Lived Download Reference",
  "description": "A short-lived URL used to fetch source media. Raw document or video bytes must never be placed in an event.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "url": {
      "type": "string",
      "format": "uri",
      "description": "Short-lived object-storage URL. Credentials must not be embedded in examples or logged."
    },
    "expiresAt": {
      "$ref": "https://schemas.m4trust.internal/ai/common/utc-timestamp/1.0.0",
      "description": "RFC 3339 UTC expiry timestamp."
    }
  },
  "required": ["url", "expiresAt"]
}
