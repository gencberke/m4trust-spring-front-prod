# Accepted plans

This directory contains plan documents whose implementation and independent
acceptance have both completed.

Move a plan here only after the planner has issued `ACCEPT` and updated
`docs/plan/CURRENT.md` where the accepted project state materially changed.
Historical hackathon plans remain under `docs/history/`; they are not migrated
into this directory.
