package com.m4trust.coreapi.fixtures.sample.apimodel;

/** Near-miss package: contains "api" as a substring prefix but is not an `.api` segment. */
public class NearMissRequestBody {
  public String value;
}
